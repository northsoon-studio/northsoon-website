---
import BaseHead from "@/components/BaseHead.astro";
import Analytics from "@vercel/analytics/astro";
import SpeedInsights from "@vercel/speed-insights/astro";

interface Props {
  title?: string;
  description?: string;
  canonical?: string;
  ogTitle?: string;
  ogDescription?: string;
  ogImage?: string;
  lang?: string;
}

const { title, description, canonical, ogTitle, ogDescription, ogImage, lang = 'es' } = Astro.props;
---

<html lang={lang}>
  <head>
    <BaseHead
      title={title}
      description={description}
      canonical={canonical}
      ogTitle={ogTitle}
      ogDescription={ogDescription}
      ogImage={ogImage}
    />
    <SpeedInsights />
    <Analytics />
  </head>
  <body class="antialiased">
    <slot />
  </body>
</html>
